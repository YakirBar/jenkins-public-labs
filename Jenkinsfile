pipeline {
    agent any
    
    stages {
        stage('pwd') {
            steps {
                sh "pwd"
            }
        }
        stage('show files:') {
            steps {
                sh "ls -la"
            }
        }
    }
}